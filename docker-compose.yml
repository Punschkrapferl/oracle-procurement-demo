services:
  oracle:
    image: gvenzl/oracle-free:latest
    container_name: oracle-procurement-oracle
    ports:
      - "1521:1521"
    environment:
      ORACLE_PASSWORD: ${ORACLE_PASSWORD:-REMOVED_OLD_ROOT_PASSWORD}
      APP_USER: ${APP_USER}
      APP_USER_PASSWORD: ${APP_USER_PASSWORD:-REMOVED_OLD_APP_PASSWORD}
    healthcheck:
      test: ["CMD", "healthcheck.sh"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
      start_interval: 5s
    volumes:
      - oracle-procurement-data:/opt/oracle/oradata

  app:
    image: punschkrapferl23/oracle-procurement-demo:1.0.0
    container_name: oracle-procurement-app
    depends_on:
      oracle:
        condition: service_healthy
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: dev
      DB_URL: jdbc:oracle:thin:@oracle:1521/FREEPDB1
      DB_USERNAME: ${APP_USER}
      DB_PASSWORD: ${APP_USER_PASSWORD:-REMOVED_OLD_APP_PASSWORD}

volumes:
  oracle-procurement-data: