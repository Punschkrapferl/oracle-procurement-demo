spring:
  datasource:
    url: ${TEST_DEMO_DB_URL:jdbc:oracle:thin:@localhost:1522/FREEPDB1}
    username: ${DEMO_DB_USERNAME}
    password: ${DEMO_DB_PASSWORD}
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration